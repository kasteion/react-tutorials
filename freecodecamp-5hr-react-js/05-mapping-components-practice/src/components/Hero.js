import React from 'react';

const Hero = () => {
    return (
        <section className="hero is-medium is-dark is-bold">
            <div className="hero-body">
                <div className="container">
                <h1 className="title">
                    Given a list of products render a Products component for each product in the list...
                </h1>
                <h2 className="subtitle">
                    As a practice on mapping components in ReactJS...
                </h2>
                </div>
            </div>
        </section>
    );
}

export default Hero;